services:
  - type: web
    name: ocr-fastapi
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false

build:
  env: python
  pythonVersion: 3.10.13
